@model IEnumerable<StarShop.Models.Produit>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nomProduit)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.minerai)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.vespene)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.picture)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nomProduit)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.minerai)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.vespene)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.picture)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idProduit }) |
            @Html.ActionLink("Details", "Details", new { id=item.idProduit }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idProduit })
        </td>
    </tr>
}

</table>
